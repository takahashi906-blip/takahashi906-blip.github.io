# 業務自動化 制作事例

## 1. Googleスプレッドシート食数管理システム

### 概要

入居者名簿、新入居予定、入院、期間欠食、食形態の一時変更を管理し、当日と翌日の食数・欠食一覧を自動生成するGoogle Apps Scriptシステムです。

### 解決する課題

- 複数シートの予定を見ながら食数を手作業で増減している
- 同じ人が複数の欠食情報に現れ、二重減算が起きやすい
- 入院・退院・取消のタイミングによって当日と翌日の値がずれる
- 処理失敗時に、どこまで完了したか分からない

### 主な機能

- 通常・一口・きざみ・ソフト・ミキサーなどの食形態別集計
- 朝食の和洋、おかゆ、昼食、おやつ、夕食への対応
- 新入居、体験入居、入院、期間欠食、取消
- 当日・翌日の食数と欠食一覧
- 入力規則、スマートフォン向け表示調整
- 処理状態、履歴、最終エラーの保存

### 技術的な工夫

- 現在値への加減算ではなく、正本データから再計算する冪等設計
- ScriptLock・DocumentLockによる同時実行制御
- 編集時は重い処理を避け、1分トリガーへ引き継ぐ構成
- 差分列のみを書き込むことで更新範囲を縮小
- DocumentPropertiesとCacheServiceによる状態・キャッシュ管理
- 段階保存、回帰テスト、手動修復関数

### 技術構成

Google Apps Script、Google Sheets、時間主導トリガー、ScriptLock、DocumentLock、DocumentProperties、CacheService

## 2. Python Webデータ集計・レポート更新ツール

### 概要

手作業で保存したHTMLや依頼者から提供されたファイルなど、取得許可のある入力を解析し、1日分または複数日分、指定項目または全項目の集計結果をGoogle Sheetsへ反映するWindows向けGUIツールです。

元の用途に依存しない構成のため、店舗別売上、商品価格、在庫、予約、アクセス数など、複数拠点・複数日の定型データ集計へ応用できます。

### 主な機能

- 基準日と対象拠点をGUIで選択
- 保存済みデータから項目・数値・日付を抽出して正規化
- 確認用CSVの出力
- Google Sheetsの指定範囲への書き込み
- 欠損日を具体的な日付で表示
- バッチファイルからの起動

### 安全性・運用

- 本更新前のドライラン
- 必要な日付が不足している場合は更新を停止
- 空データによる誤上書きを防止
- 書き込み範囲を必要な列へ限定
- サービスアカウント鍵をコード・配布物から分離

### 技術構成

Python、BeautifulSoup、Google Sheets API、Google Drive API、デスクトップGUI、Windowsバッチ

データ取得は公式API、依頼者提供ファイル、手作業で保存したデータなど、利用許可と規約を確認できる方法に限定します。認証回避やアクセス制限の回避は行いません。

## 3. CSVからGoogle Sheetsへの自動取込

UTF-8またはBOM付きCSVを検証し、同じ日付・レコードIDを重複登録せず、指定したGoogle Sheetsへ保存します。ドライラン、サービスアカウント認証、Windowsタスクスケジューラからの定期実行に対応します。

## 4. Excel帳票・配置図の自動作成

管理番号、設備名、設置位置などの構造化データから、色分けした配置図、分類別の凡例、集計欄を含むExcelファイルを自動生成します。座席表、倉庫配置図、設備台帳などの定型フォーマットへ横展開できます。

## 対応できる案件

- GAS・Googleスプレッドシートの修正、集計、転記、トリガー
- PythonによるCSV・HTML解析、データ整形、GUI
- Google Sheets / Drive API連携
- Excel帳票、一覧表、配置図の自動作成
- 既存ツールの不具合調査、機能追加、手順書作成
- 定期実行、重複防止、処理履歴、エラー記録

## 公開時の注意

公開画面はサンプルデータのみを使用します。実在する氏名、非公開の拠点情報、サービスアカウント鍵、APIキー、スプレッドシートIDは共有しません。
